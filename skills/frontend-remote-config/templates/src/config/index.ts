import appConfig from 'virtual:app-config'

/**
 * Config da aplicação resolvida em build/dev (ver `vite/app-config.ts`).
 * Constante síncrona — importe e use diretamente: `appConfig.apiBaseUrl`.
 */
export { appConfig }
export type { AppConfig } from './types'
